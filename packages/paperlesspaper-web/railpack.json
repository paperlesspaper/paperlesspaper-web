{
  "provider": "node",
  "packages": {
    "node": "24"
  },
  "steps": {
    "install": {
      "commands": [
        {
          "src": ".npmrc.nixpacks",
          "dest": ".npmrc.nixpacks"
        },
        "cp .npmrc.nixpacks \"$HOME/.npmrc\" && NPM_CONFIG_USERCONFIG=\"$HOME/.npmrc\" yarn install && rm -f \"$HOME/.npmrc\" .npmrc.nixpacks"
      ]
    },
    "build": {
      "commands": ["yarn build:production"]
    }
  }
}
