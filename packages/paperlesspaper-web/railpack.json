{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "24"
  },
  "steps": {
    "install": {
      "commands": [
        "cp .npmrc.nixpacks \"$HOME/.npmrc\" && NPM_CONFIG_USERCONFIG=\"$HOME/.npmrc\" yarn install && rm -f \"$HOME/.npmrc\""
      ]
    },
    "build": {
      "commands": [
        "yarn build:production"
      ]
    }
  }
}